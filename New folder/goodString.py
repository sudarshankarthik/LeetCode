def countGoodStrings(self, low: int, high: int, zero: int, one: int) -> int:
    
    def res(count):
        if count > high:
            return 0
        
        if count >= low:
            res = 1
        else:
            res = 
        
        return res()



low = 3
high = 3
zero = 1
one = 1
print(countGoodStrings(1,low,high,zero,one))